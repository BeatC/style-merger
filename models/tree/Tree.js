'use strict';

var node = require("./Node.js");

function Tree (root) {
	this.root = root;
}
Tree.prototype.merge = function(first_argument) {
	// body...
};
Tree.prototype.add = function(first_argument) {
	// body...
};
Tree.prototype.remove = function(first_argument) {
	// body...
};
Tree.prototype.getDocument = function(first_argument) {
	// body...
};